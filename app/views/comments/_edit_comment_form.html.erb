<%= form_for @comment, html: {multipart: true}, remote: true do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: t("review.edit_comment") %>
  </div>
  <%= f.submit t("review.edit"), class: "btn btn-primary" %>
<% end %>
